## Translation Providers

* DeepL integration — translate content records via the DeepL API
* OpenAI integration — alternative translation backend via OpenAI Chat API
* Configurable provider — switch between DeepL and OpenAI via extension settings

## Backend Integration

* Backend button — inline translate button on content element records
* Language fallback — translates from the default language to any configured site language
